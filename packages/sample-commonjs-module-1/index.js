function square(x) {
    return x * x;
}

function squareRoot(x) {
    // some comment
    return Math.sqrt(x);
}

// some comments to trigger version change

exports.square = square;
exports.squareRoot = squareRoot;
